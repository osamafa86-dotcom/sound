# خطة مشروع موقع الصوتيات بالذكاء الاصطناعي

> وثيقة التخطيط المعتمدة للمشروع — تُحدَّث مع تقدم العمل.

## 1. فكرة الموقع

منصة ويب عربية متخصصة في الصوتيات المولّدة بالذكاء الاصطناعي، مقسومة إلى قسمين رئيسيين:

| القسم | الوظيفة |
|---|---|
| **استوديو النص إلى صوت (TTS)** | المستخدم يكتب نصاً، يختار الصوت واللهجة والإعدادات، ويحصل على ملف صوتي عالي الجودة |
| **استوديو الأغاني والمقامات** | المستخدم يكتب (أو يولّد بمساعدة الذكاء الاصطناعي) كلمات أغنية، يختار المقام والإيقاع والأسلوب، ويحصل على أغنية كاملة بجودة عالية |

اللغة الأساسية للواجهة: **العربية (RTL)** مع إمكانية إضافة الإنجليزية لاحقاً.

---

## 2. خدمات الذكاء الاصطناعي المقترحة (الـ APIs)

### أ. قسم تحويل النص إلى صوت (TTS)

| الخدمة | الدور | لماذا؟ |
|---|---|---|
| **ElevenLabs** | المزوّد الأساسي | أفضل جودة صوت وواقعية في السوق، يدعم العربية، استنساخ أصوات، تحكم بالتعبير والنبرة، API ممتاز وسهل |
| **Azure AI Speech** | المزوّد الثاني (اللهجات) | أوسع تغطية للهجات العربية (~20 لهجة إقليمية: سعودية، مصرية، أردنية، إماراتية، عراقية، مغربية، لبنانية...) مع تحكم دقيق عبر SSML وسعر منافس |
| Google Cloud TTS / OpenAI TTS | احتياطي/توسعة مستقبلية | تنويع المزودين وخفض التكلفة عند الحاجة |

**الخيارات المتقدمة التي سنوفرها للمستخدم:**
- اختيار المزوّد والصوت (ذكر/أنثى، عمر، طابع الصوت)
- اللهجة (فصحى + لهجات إقليمية عبر Azure)
- السرعة، طبقة الصوت، درجة الثبات مقابل التعبير العاطفي
- وقفات وتشكيل النطق (عبر SSML)
- صيغة التنزيل (MP3 / WAV) وجودة الإخراج
- معاينة فورية + حفظ في مكتبة المستخدم

### ب. قسم كتابة وتلحين الأغاني (المقامات)

هنا نستخدم **طبقتين** — وهذا سرّ الجودة:

**الطبقة 1 — كتابة الكلمات واختيار المقام: Claude API (Anthropic)**
- مساعدة المستخدم في كتابة/تحسين كلمات الأغنية (فصحى أو لهجة)
- اقتراح المقام المناسب للمعنى والمزاج (بياتي، حجاز، راست، صبا، كرد، نهاوند، عجم، سيكاه...)
- تحويل اختيارات المستخدم إلى «برومبت موسيقي احترافي» لمحرك التوليد
- ملاحظة مهمة: محركات توليد الموسيقى لا تفهم «المقام» كمعامل تقني مباشر، بل عبر وصف الأسلوب في البرومبت (مثل: "Arabic maqam Bayati, quarter tones, oud & qanun, darbuka rhythm, classical tarab"). طبقة الذكاء الاصطناعي الوسيطة هي التي تصنع هذا الوصف بإتقان.

**الطبقة 2 — توليد الأغنية نفسها:**

| الخدمة | الحالة | التقييم |
|---|---|---|
| **Eleven Music API (ElevenLabs)** | ✅ API رسمي متاح الآن | الخيار المعتمد للأغاني **بغناء عربي**: مرخّص تجارياً (مدرّب على موسيقى مرخصة بالتعاون مع شركات إنتاج)، يدعم الغناء بعشرات اللغات منها العربية، السعر ~‏$0.15 لكل دقيقة موسيقى عبر الـ API. **تحديث (تموز 2026):** أصدرت ElevenLabs نموذج **Music v2** (متاح API منذ 15 حزيران) والافتراضي لديهم بقي v1 انتقالياً — اعتمدنا v2 افتراضياً عندنا عبر مسار البرومبت (بنية المقاطع والكلمات داخل النص، إخراج 48kHz/192kbps)، والترقيع الجزئي source_from يبقى على خطة v1 المثبتة، وELEVEN_MUSIC_MODEL مفتاح رجوع |
| **Google Lyria 3 / Lyria 3 Pro (Gemini API)** | ✅ متاح عبر Gemini API (معاينة عامة) | جودة عالية 44.1kHz ستيريو مع غناء وكلمات موقوتة وبنية أغنية كاملة؛ Lyria 3 مقاطع 30 ثانية وLyria 3 Pro أغانٍ حتى ~3 دقائق؛ سعر ممتاز (~$0.08 للأغنية الكاملة)؛ يدعم أيضاً التوليد من صورة. **تحديث (تموز 2026):** أطلقت جوجل **Flow Music** (flowmusic.app — منتج رسمي، خلفه ProducerAI المستحوذ عليها) على محرك Lyria 3 Pro نفسه الذي نستخدمه عبر Gemini API، ووثائق Gemini API لم تعد تحصر لغات الغناء («يولّد الغناء بلغة البرومبت») — لذا فعّلنا **وضع المقارنة التجريبي** للمسجلين لاختبار الغناء العربي عملياً واعتماد الأفضل بالأدلة (لا يوجد API لتطبيق Flow Music ذاته؛ قناة المطورين الرسمية هي Gemini API التي نحن عليها) |
| **Suno** | ⚠️ لا يوجد API رسمي عام حتى الآن (تموز 2026) | الأفضل جودةً للأغاني العربية ويفهم مصطلحات المقامات في البرومبت، لكن الوصول متاح فقط عبر وسطاء غير رسميين (مخاطرة قانونية واستقرار) — نراقب إطلاق الـ API الرسمي وندمجه فور توفره |
| Mureka / MiniMax Music / Stable Audio | بدائل | للتنويع وخفض التكلفة مستقبلاً |

**استراتيجية الاستفادة من Lyria 3 في موقعنا:**
1. **الموسيقى الآلية بالمقامات** (بدون غناء): عزف عود وقانون وناي بمقام محدد — لا يتأثر بقيد اللغات، وسعره زهيد.
2. **مستوى المعاينة الرخيص:** المستخدم يولّد مسودة 30 ثانية بـ Lyria 3 ليجرب المقام والأسلوب، ثم يولّد النسخة النهائية الكاملة بالمزوّد الأنسب — يوفّر التكلفة كثيراً.
3. **التوليد من صورة:** ميزة تسويقية فريدة (ارفع صورة واحصل على موسيقى تناسب أجواءها).
4. **الأغاني المغنّاة بالعربية:** تبقى على Eleven Music حالياً، مع تجربة العربية على Lyria دورياً واعتمادها فور الدعم الرسمي.

**قرار معماري مهم:** نبني «طبقة تجريد» (Provider Abstraction) بحيث يكون كل مزوّد مجرد وحدة قابلة للتبديل — إذا أطلق Suno الـ API الرسمي أو ظهر مزود أفضل، نضيفه دون إعادة بناء.

---

## 3. البنية التقنية

| المكوّن | الاختيار | السبب |
|---|---|---|
| الواجهة | **Next.js + React + Tailwind CSS** | دعم RTL ممتاز، أداء عالٍ، مناسب للنشر السريع |
| الخادم | Next.js Route Handlers | نفس المشروع، يبسّط البداية |
| قاعدة البيانات + المصادقة + التخزين | **Supabase** (Postgres + Auth + Storage) | حل واحد متكامل: حسابات المستخدمين، حفظ الملفات الصوتية، سجل التوليدات |
| المهام الطويلة | جدول Jobs + Webhooks/Polling | توليد الأغنية يستغرق وقتاً؛ المستخدم يرى حالة «قيد التوليد» ثم يصله الناتج |
| الاستضافة | Vercel | تكامل مباشر مع Next.js |
| الدفع (لاحقاً) | نظام كريدت + Stripe أو بديل إقليمي | تغطية تكلفة الـ APIs |

### هيكل الصفحات
```
/                    الصفحة الرئيسية (تعريف بالقسمين)
/tts                 استوديو النص إلى صوت
/songs               استوديو الأغاني والمقامات (3 خطوات: كلمات ← مقام وأسلوب ← توليد)
/library             مكتبتي (المحفوظات والتنزيلات)
/pricing             الأسعار والكريدت
/login /signup       الحسابات
```

---

## 4. مراحل التنفيذ

- [x] **المرحلة 0 — التأسيس:** مشروع Next.js + Tailwind + RTL + الهوية البصرية والصفحة الرئيسية ✓ (تم: صفحات رئيسية + استوديوهان بوضع تجريبي يعزف سلّم المقام + طبقة تجريد المزوّدين + مسارات API)
- [x] **المرحلة 1 — استوديو TTS:** تكامل ElevenLabs يعمل حياً على الإنتاج ✓ (اختُبر على sound-five-inky.vercel.app: توليد كلام عربي MP3 بمحرك elevenlabs الحقيقي، وتوليد موسيقى آلية بمقام الحجاز بمحرك eleven-music) — كتالوج من 22 صوتاً عربياً أصلياً (متحدثون أصليون بلهجات فلسطينية وأردنية وشامية وسورية ولبنانية وفصحى ومصرية وسعودية وخليجية) + 8 أصوات Azure Neural جاهزة تتفعل بمفتاح AZURE_SPEECH_KEY، مع تقسيم النصوص الطويلة حتى 20 ألف حرف عند حدود الجمل ودمج المخرجات (أساس الكتب الصوتية)
- ملاحظة نشر: الموقع منشور على Vercel (فريق sound/مشروع sound) بخطة Hobby، والدومين sound.feedsnews.net مربوط بانتظار تعديل DNS في GoDaddy
- [x] **المرحلة 2 — مساعد الكلمات والمقامات:** يعمل حياً على الإنتاج ✓ — بالأولوية: Claude (مخرجات منظّمة على Opus 5 مع رجوع خادمي عند رفض مصنّفات الأمان) ← Gemini (gemini-3.6-flash عبر responseSchema) ← الوضع التجريبي؛ تأليف الكلمات من فكرة، تحسين كلمات المستخدم، اقتراح المقام مع التعليل، وبناء البرومبت الموسيقي الاحترافي
- [x] **المرحلة 3 — توليد الأغاني:** يعمل حياً على الإنتاج ✓ — **Google Lyria 3 Pro** محركاً أساسياً (MP3 ستيريو 192kbps/44.1kHz، ~$0.08 للمقطع) مع سلسلة رجوع تلقائي إلى Eleven Music ثم الوضع التجريبي وإعادة محاولة على ازدحام 503، **ونظام مهام طويلة كامل**: إنشاء Job واستعلام دوري ثم جلب الصوت، بتنفيذ خلفي عبر after() ومخزن دائم في Supabase (بديل الذاكرة للتطوير)، ومستوى توليد بمعاينة سريعة ~30 ثانية أو نسخة كاملة بمدة يختارها المستخدم، **ووضع مقارنة تجريبي للمسجلين**: كل توليد كامل يخرج نسختين متوازيتين (Lyria 3 Pro × Eleven Music) جنباً إلى جنب للحكم على الجودة والأداء والنطق العربي، واعتماد الأفضل يبث إشارتي version_chosen/version_rejected لعقل المنصة ويستهلك توليدَين من الحد — **وبعد جولة التجربة الحية عاد الافتراضي إلى نسخة واحدة بالسلسلة الكاملة** (Lyria 3 Pro أساسياً مع رجوع تلقائي إلى Eleven Music v2 — فرض محرك واحد لكل نسخة في المقارنة يُفقد شبكة الأمان فيتراجع الأداء المحسوس)، والمقارنة تبقى زراً اختيارياً يفعّله المستخدم عند رغبته بالحكم بنفسه
- [~] **المرحلة 4 — الحسابات والمكتبة:** مكتملة برمجياً — Supabase Auth (دخول/تسجيل + حالة الجلسة في الشريط العلوي)، حفظ التوليدات (حاوية تخزين خاصة + جدول generations بحماية RLS، مخطط SQL جاهز في supabase/schema.sql)، صفحة مكتبة فعلية بالتشغيل والحذف، مكتبة محلية (IndexedDB) تعمل تلقائياً بدون حساب، وحفظ خادمي مباشر لمهام الأغاني — التفعيل السحابي بانتظار إنشاء مشروع Supabase ووضع مفاتيحه الثلاثة
- [x] **المرحلة 5 — صلابة الإنتاج (Vercel):** حماية استهلاك موحّدة بطبقتين (حد لكل زائر/مستخدم + سقف عام يحمي المحفظة) بمخزن ذرّي في Supabase وبديل محلي، حدود مضاعفة للمسجلين عبر Bearer token، مهلات موسّعة للمسارات، اختبارات Vitest وCI على GitHub Actions — يتبقى: Sentry للمراقبة (مرحلة النمو)
- [ ] **المرحلة 6 — الكريدت والدفع:** دفتر رصيد (قيود مزدوجة)، خصم مقدّر قبل التوليد وتسوية بعده، Stripe + بوابة إقليمية (مدى/محافظ)، تفعيل صفحة الأسعار
- [~] **المرحلة 7 — استوديو الصوت المتقدم:** مكتمل برمجياً — صفحة «معمل الصوت» بتسجيل داخل المتصفح أو رفع ملف، تفريغ نصي عبر Scribe (مُختبَر حياً بدقة تامة)، تحويل صوت إلى صوت، استنساخ صوتي بموافقة صريحة إلزامية وربط بالمالك (جدول custom_voices — للمسجلين حصراً على الإنتاج)، تصميم أصوات عربية جديدة من وصف نصي مع معاينات، وذاكرة نطق متراكمة (كتابةً أو تسجيلاً بالصوت) تُطبَّق تلقائياً على كل توليد — يتبقى: البث الفوري أثناء التوليد
- [~] **المرحلة 8 — استوديو الموسيقى المتقدم:** تعديل المقاطع وإعادة التوليد الجزئي والتوليد من صورة تعمل حياً؛ **فصل المسارات (Stems)** يعمل: الغناء وحده (أكابيلا) عبر عازل ElevenLabs، والنسخة الآلية فورياً في المتصفح بإلغاء القناة الوسطى مع الإبقاء على الباص (صفر كلفة)؛ **غناء المستخدم على اللحن المولّد** يعمل: تُستخلص النسخة الآلية وتُعزف أثناء تسجيل صوت المستخدم، مع تنقية اختيارية عبر العازل وتحكم بعلو الصوت وتعويض تأخير التسجيل، ومزج محلي إلى WAV بحماية من التشبع وحفظ في المكتبة؛ **ومحرر النص والصوت**: الكلمات تُزامَن تلقائياً فور اكتمال كل توليد مغنّى وتضيء مع الغناء، وكل كلمة قابلة للضغط — تُسمَع وحدها، ويُصحَّح نطقها في مكانها (يتعلمه العقل للأبد)، ويُعاد إنشاد مقطعها فقط دون باقي الأغنية، والمقطع يُحدَّد بالخط الزمني للمقاطع لا بمطابقة النص (الزمن أصدق من التفريغ)؛ **والتشكيل التلقائي قبل التلحين**: كل كلمات مغناة عارية من الحركات تُشكَّل تلقائياً تشكيلاً تاماً حسب اللهجة داخل منفّذ المهمة قبل وصولها للمحرك (فشل المدقق لا يوقف التوليد)، والنص المشكّل كما غُنّي فعلاً يعود للواجهة فتتبناه، والمحرر يعرض كلمات التفريغ بصورتها المكتوبة المشكّلة عبر محاذاة زمنية — يتبقى: فصل مسارات متعدد الآلات (طبول/باص/آلات) عبر مزوّد متخصص، جاهزية Suno API
- [~] **المرحلة 9 — النمو:** **لوحة مالك النظام مكتملة** (`/admin`) — صلاحية بمتغير البيئة `OWNER_EMAILS` لا بقاعدة البيانات (فلا يمنح أحد نفسه الصلاحية)، وسبعة تبويبات: صحة النظام والتكاملات وحالة كل ميزة وبيئة النشر، الاستهلاك والكلفة التقديرية حتى ٩٠ يوماً، المستخدمون وأرصدتهم، مهام التوليد وأسباب فشلها، الحدود ونوافذها الحيّة مع تصفير فوري، كتالوج الأصوات وبنك الأصوات المخصصة، ومفاتيح تحكم حيّة (صيانة، وضع تجريبي قسري، إيقاف خدمة بعينها، لافتة الموقع) تسري خلال ثوانٍ بلا إعادة نشر مع سجل تدقيق كامل — يتبقى: مشاركة برابط عام، مشاريع، Sentry وتحليلات، API للمطورين، لغة إنجليزية

### عقل المنصة (طبقة التعلّم الذاتي)

بدل تدريب نموذج صوتي من الصفر (غير واقعي: آلاف ساعات التسجيل + بنية GPU)، نبني طبقة تعلّم فوق المحركات تتحسّن مع كل استخدام:

| العضو | ما يتعلّمه | الحالة |
|---|---|---|
| **1. ذاكرة النطق** | تصحيحات نطق الكلمات العربية، تُطبَّق تلقائياً على كل توليد لاحق ولكل الأصوات | ✅ يعمل (قاموس ElevenLabs موحّد + واجهة تعليم) |
| **2. حلقة التقييم** | أي صوت/إعدادات ترضي المستخدمين لكل لهجة ونوع محتوى → ترتيب تلقائي للأصوات | ⏳ يحتاج قاعدة بيانات (المرحلة 4) |
| **3. الضبط الذاتي** | أفضل قيم الثبات/السرعة لكل صوت ونوع نص، تُعدَّل من بيانات التقييم | ⏳ يحتاج قاعدة بيانات |
| **4. بنك الأصوات المتنامي** | الأصوات المصممة والمستنسخة تتراكم، والأعلى تقييماً يتصدّر الكتالوج | ✅ جزئياً (تصميم + استنساخ يعملان) |
| **5. تطوّر البرومبت الموسيقي** | البرومبتات التي أنتجت أفضل الأغاني تُحقن كأمثلة في مساعد الكلمات | ⏳ يحتاج قاعدة بيانات |

---

### عدالة الرصيد (آب 2026)

مراجعة شاملة متعددة الوكلاء لقسم الأغاني أكّدت سبع ثغرات وأُغلقت كلها: استرداد تلقائي للنقاط عند الفشل أو الناتج التجريبي، فصل التوليد عن التخزين (عطل رفع عابر لا يُهدر توليداً مدفوعاً — إعادة محاولة ثلاثية)، حارس كسول للمهام العالقة (٨ دقائق ← فشل معلن + استرداد)، استبدال كلمة كاملة في تصحيح النطق (لا تصيب «منها» عند تعليم «من»)، إصلاح إغلاق متقادم في تصحيح النص الحر، حارس تقادم للكاريوكي، ودفعة إتاحة (radiogroup للمقامات، aria-current، aria-live). وتنظيف برومبتات: كلمات Lyria بلا ترويسات عربية، إزالة ازدواج «Arabic dialect»، وعزل إشارات المقارنة عن درجات سلالات البرومبتات.

### قرار المالك (آب 2026): Lyria حصراً لتوليد الأغاني

بعد التحكيم بالسمع عبر توليدات كثيرة حكم المالك أن Lyria أعلى وأقوى من Eleven Music في الأغاني — فاعتُمد **فرض Lyria وحده** لتوليد الأغاني عبر `MUSIC_PROVIDER=lyria` في بيئة Vercel (أحدث نموذج `lyria-3-pro-preview` للكاملة و`lyria-3-clip-preview` للمعاينة). التبعات الموثقة: كلمات يرفضها مرشّح محتوى Google تخرج سلّم مقام تجريبياً مع عرض السبب (بلا رجوع لمحرك بديل)، والرجوع عن القرار حذف المتغير فقط. مفتاح ElevenLabs باقٍ كما هو لبقية الأعضاء: TTS والكتالوج، معمل الصوت (استنساخ/تحويل/عزل)، الكاريوكي والتفريغ، ذاكرة النطق، والأكابيلا — ووضع المقارنة الاختياري ما زال قادراً على استدعاء Eleven عند تفعيله يدوياً لإعادة التحكيم.

### جودة الإنتاج المسموعة (آب 2026 — المرحلة ب)

- **ماستر بمعيار البث**: قياس LUFS متكامل وفق ITU-R BS.1770 (ترشيح K + بوابتان) وضبط نحو ‑14 LUFS كما تطبّع منصات البث، مع محدد ذروة بنظرة أمامية (~-1dB) بدل تطبيع الذروة الأعمى — نواة نقية مختبرة في `src/lib/loudness.ts`، وقص المقدمة يعيد مقداره فتُزاح توقيتات الكاريوكي بدل أن تنحرف.
- **حدود المقاطع الحقيقية**: تُقاس من مطابقة كلمات كل مقطع مع التفريغ الموقوت (`measureSectionStarts`) بدل جمع المدد المخططة التي يتجاهلها Lyria — فخريطة محرر النص والصوت وإعادة إنشاد المقطع تصيب مقطعها الفعلي.
- **معايرة كمون تلقائية** في «غنِّ بصوتك»: لحظة بدء العزف الفعلي + كمون إخراج الجهاز يضبطان تقديم التزامن تلقائياً بدل تخمين المستخدم.

### حماية الاستهلاك (تشغيلي)

الموقع عام بلا حسابات، وكل طلب توليد يستهلك رصيداً مدفوعاً. لذا سياج حدود على كل المسارات المكلفة:

| المسار | لكل زائر | سقف المنصة | النافذة |
|---|---|---|---|
| النص إلى صوت | 20 | 300 | ساعة |
| توليد الأغاني | 5 | 60 | ساعة |
| مساعد الكلمات | 30 | 400 | ساعة |
| تصميم الأصوات | 5 | 40 | ساعة |
| استنساخ الصوت | 3 | 20 | يوم |
| ذاكرة النطق | 30 | 200 | ساعة |

السقف العام هو صمام أمان المحفظة: يوقف التوليد للجميع مؤقتاً إذا تجاوزت المنصة حدها، حتى لو كان كل زائر ضمن حده.
التخزين ذرّي في Supabase على الإنتاج (دالة consume_rate_limit)، وذاكرة النسخة بديل في التطوير. المسجلون يحصلون على ثلاثة أضعاف حد الزائر. تُضاف مسارات التفريغ (8/ساعة) والتحويل الصوتي (6/ساعة) لنفس السياج.

فوق هاتين الطبقتين **طبقة ثالثة يملكها صاحب النظام** من لوحة `/admin`: وضع صيانة يوقف كل التوليد، أو إيقاف خدمة بعينها، أو وضع تجريبي قسري يُبقي الموقع عاملاً بصفر إنفاق. تُفحص هذه الطبقة داخل `checkLimit` نفسها فتغطي كل المسارات المكلفة تلقائياً، وتسري خلال ١٥ ثانية على كل نسخ الخادم بلا إعادة نشر.

---

## 5. التكاليف التشغيلية التقريبية

| البند | التكلفة التقريبية |
|---|---|
| ElevenLabs TTS | باقات من $5–$22 شهرياً للبداية، أو حسب الاستهلاك عبر API |
| Eleven Music | ~‏$0.15 لكل دقيقة موسيقى مولّدة |
| Google Lyria 3 Pro (Gemini API) | ~‏$0.08 للأغنية الكاملة |
| Azure TTS | ~‏$16 لكل مليون حرف (أصوات Neural) |
| Claude API | زهيدة جداً لكتابة الكلمات والبرومبتات |
| Supabase + Vercel | خطط مجانية كافية للانطلاق |

---

## 6. المطلوب من صاحب المشروع قبل البدء بالتكاملات

1. **مفتاح API من ElevenLabs** (يغطي TTS + Music معاً — الأولوية الأولى)
2. مفتاح **Anthropic (Claude)** لمساعد الكلمات والمقامات
3. مفتاح **Gemini API (Google AI Studio)** لموديلات Lyria 3 — إنشاؤه مجاني وسريع
4. اختيارياً: حساب **Azure** لأصوات اللهجات العربية
5. اسم الموقع / النطاق (الدومين) المطلوب
6. أي تفضيلات في الهوية البصرية (ألوان، طابع التصميم)

> ملاحظة: يمكن البدء بالمرحلتين 0 و1 من الواجهة فوراً حتى قبل توفر المفاتيح، باستخدام وضع تجريبي (Mock) ثم تفعيل المفاتيح لاحقاً.
