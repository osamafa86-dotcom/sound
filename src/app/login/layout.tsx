import type { ReactNode } from "react";

export const metadata = {
  title: "الدخول وإنشاء حساب",
  description: "سجّل مجاناً لتحفظ أعمالك وتحصل على رصيد شهري متجدد.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
