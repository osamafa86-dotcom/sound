import type { ReactNode } from "react";

export const metadata = {
  title: "معمل الصوت",
  description: "فرّغ تسجيلاتك نصياً، حوّل أداءك لصوت آخر، اعزل الضجيج، أو استنسخ نسختك الرقمية.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
