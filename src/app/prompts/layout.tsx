import type { ReactNode } from "react";

export const metadata = {
  title: "وكيل البرومبتات",
  description: "وكيل ذكاء اصطناعي يصوغ برومبتات احترافية لأي منصة — صياغة ونقد وتحكيم آلي حتى أعلى جودة.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
