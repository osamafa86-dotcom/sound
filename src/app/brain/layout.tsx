import type { ReactNode } from "react";

export const metadata = {
  title: "عقل المنصة",
  description: "شفافية كاملة: كيف تتعلم مقام من تقييماتك واستماعك لتتحسن تلقائياً كل أسبوع.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
