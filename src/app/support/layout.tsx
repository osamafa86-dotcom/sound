import type { ReactNode } from "react";

export const metadata = {
  title: "الدعم والتواصل",
  description: "فريق مقام يسمعك — مشكلة تقنية، ترقية باقة، أو اقتراح ميزة.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
