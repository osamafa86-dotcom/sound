import type { ReactNode } from "react";

export const metadata = {
  title: "مساحة مقام — استوديو البطاقات",
  description:
    "ركّب خط إنتاجك الصوتي بنفسك: بطاقات كلمات وتشكيل وصوت وأغنية تتوصل بالسحب وتعمل بالتسلسل — أول مساحة عقد عربية للإنتاج الصوتي.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
